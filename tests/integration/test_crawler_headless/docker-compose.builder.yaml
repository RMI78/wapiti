version: '3.9'

services:
  websites_builder:
    build:
      context: '.'
      dockerfile: Dockerfile.builder
    volumes:
      - ./vue-memHist-built:/home/node/vue-memHist
      - ./vue-webHist-built:/home/node/vue-webHist
      - ./vue-website:/home/node/website
      - .node_modules:/home/node/website/node_modules

volumes:
  node_modules:
